import TabOneContainer from "../../components/tab-one/one.container";
const TabOneScreen: React.FC<TabOneContainerProps> = (props) => (
  <TabOneContainer {...props} />
);
export default TabOneScreen;
